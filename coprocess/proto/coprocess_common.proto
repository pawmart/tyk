syntax = "proto3";

package coprocess;

enum HookType {
  Unknown = 0;
  Pre = 1;
	Post = 2;
	PostKeyAuth = 3;
	CustomKeyCheck  = 4;
}

message StringSlice {
  repeated string items = 1;
}

option optimize_for = LITE_RUNTIME;
option cc_enable_arenas = true;
